const mongoose = require("mongoose");

const chatSchema = new mongoose.Schema(
    
)

// message inside the event (from, to, timestamp, postid, message)

// api/messages?postid=2&orderBy=timestamp&orderDirection=asc&page=1&size=50
